type: object
description: A complete slide with all its rows
properties:
  slide_id:
    type: string
  position:
    type: integer
  slide_rows:
    type: array
    items:
      $ref: "./SlideRowView.yaml"
required:
  - slide_id
  - position
  - slide_rows
